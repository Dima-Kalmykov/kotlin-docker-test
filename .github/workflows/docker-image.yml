name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'
          cache: gradle

      - name: build with gradle
        run: gradle clean build

      - name: build & push docker image
        uses: mr-smithers-excellent/docker-build-push@v5
        with:
          image: cteklo/kotlin-docker
          tags: ${{ github.run_number }}
          registry: docker.io
          dockerfile: Dockerfile
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: deploy
        uses: appleboy/ssh-action@master
        env:
          LATEST_VERSION: ${{ github.run_number }}
        with:
          host: 149.154.64.151
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          envs: LATEST_VERSION
          script: |
            echo ::set-env name=PREVIOUS_VERSION::$(($LATEST_VERSION-1))
            docker rm $(docker stop $(docker ps -a -q --filter ancestor=cteklo/kotlin-docker:$PREVIOUS_VERSION --format="{{.ID}}"))
            docker run -d -p 5005:5005 -p 8081:8081 cteklo/kotlin-docker:$LATEST_VERSION 
